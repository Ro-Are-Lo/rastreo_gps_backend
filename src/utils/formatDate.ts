/*
Funciones auxiliares y helpers

Aquí van funciones genéricas o reutilizables, por ejemplo:

Formatear fechas

Encriptar contraseñas

Validar correos o tokens

Generar UUID

// Función auxiliar para dar formato a fechas
export const formatDate = (date: Date) => {
  return date.toISOString().split('T')[0];
};



*/