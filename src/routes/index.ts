
/*
import { Application } from 'express';
import driverRoutes from './driver.routes';

// Centraliza todas las rutas del proyecto
export const registerRoutes = (app: Application) => {
  app.use('/api/drivers', driverRoutes);
};
*/